# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
  config.vm.box = "almalinux8-basebox"

  # VirtualBox provider configuration
  config.vm.provider "virtualbox" do |vb|
    vb.name = "Almalinux 8 - Basebox"
    vb.memory = "512"
    vb.cpus = 2
  end

end
